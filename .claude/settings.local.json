{
  "permissions": {
    "allow": [
      "Bash(docker-compose up *)",
      "Bash(docker-compose ps *)",
      "Bash(curl -s http://localhost:8000/items)",
      "Bash(curl -s -X POST http://localhost:8000/items -H \"Content-Type: application/json\" -d '{\"name\": \"Test Item\", \"description\": \"just checking if it works\"}')"
    ]
  }
}
