version: "3.8"
services:
  db:
    image: mongo:7
    volumes:
      - mongodata:/data/db

  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      MONGO_URL: mongodb://db:27017
    depends_on:
      - db

volumes:
  mongodata:
